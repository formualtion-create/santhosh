{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cp prisma/schema.postgres.prisma prisma/schema.prisma && npx prisma db push --skip-generate --accept-data-loss && npm run build && (node prisma/seed.mjs || echo 'seed skipped')",
  "crons": [
    { "path": "/api/cron/daily-thought", "schedule": "0 9 * * *" }
  ]
}
